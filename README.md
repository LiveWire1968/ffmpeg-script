# ffmpeg-script
FFMpeg that converts to HEVC , shows status bar AND ffmpeg output. Searchs entire directory and subdirectories for .mkv , .mp4 , and .avi. Will not convert files that are already HEVC. Deletes old file (CAUTION). Script is setup for nvidia nvenc.
